PORT=4000
MONGO_URI=mongodb://127.0.0.1:27017/omniwork-revival
JWT_SECRET=change-this-in-real-deployment
CLIENT_ORIGIN=http://localhost:5173

# Optional — if unset, the AI orchestrator falls back to canned responses
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-sonnet-4-6
